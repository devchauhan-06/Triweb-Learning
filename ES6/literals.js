console.log(0b111110111 === 503)  //the == operator does the type conversion of the operands before comparison, whereas the === operator compares the values as well as the data types of the operands.
console.log(0o767 === 503)
//no need for type conversion u can straightaway compare ocatal/binary with decimal

